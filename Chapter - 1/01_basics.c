// Variables are just container that are used to store the values of any int ,float and char type in them

#include<stdio.h>

int main(){
  // variable names are only start with underscore and an alphabet
  // ** NO variable is start with any number, special symbol, or any space between the words
  int _a = 7;
  // variable names are case sensitive 
  int num = 5;
  int Num = 5 ;
  // both are different and don't cause any error
  printf("Hello world");
  return 0;
}