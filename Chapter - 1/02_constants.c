#include<stdio.h>
// constants are one whose value doesn't change

int main(){
  // 3 types of constants are present which were
  // integer , float and character constants

  int val1 = 3;

  float val2 = 6.32;

  char ch = 'c';

  printf("val1 = %d, val2 = %.2f, ch = %c", val1,val2,ch);

  return 0;
}