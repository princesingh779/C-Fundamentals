//header files are included which consist the defination of the funcitons and methods used earlier in the program
#include<stdio.h>

// when the program run it finds the main in the program and start executing form that statement
int main(){
  int val =4 ;
  printf("val = %d",val);
  return 0;
}